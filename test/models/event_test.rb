require 'test_helper'

class FeedbackTest < ActiveSupport::TestCase

	# event members should not be duplicated
	test 'event members no duplicates' do 
	end
	# should not be able to import duplicate google events
end
